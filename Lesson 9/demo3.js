let arr = [1, 2, 3, 4, 5, 6, 7, 8];


//  arr.forEach(/*<call this action>*/)
arr.forEach(
    function (element) {
        console.log(element);
    }
);

/**
 * 
 * function forEach(cb) {
 *      
 *      for(let =0; i<length; i++) {
 *          cb(arr[i]);
 *      }
 * 
 * }
 * 
 */

